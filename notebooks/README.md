# Notebooks

| File | Description |
|------|-------------|
| `data_formatting.ipynb` | Converts raw SMNI CMI individual trial files into the wide-format CSV used by the pipeline. Run this first if starting from the raw download. |
| `feat_extraction_original.ipynb` | Original exploratory notebook from the course project. Uses MNE for bandpass/notch filtering and REST re-referencing. Kept for reference — not part of the main pipeline. |
